\documentclass{standalone}
% Preamble
\begin{document}

\begin{abstract}
Let $\mathbb{Q}[x]$ be the algebra of polynomials, with rational coefficients in the variables $x = x_1, \ldots, x_n$.
Given in $\mathbb{Q}[x]$ a zero-dimensional ideal $\left< f\right> = \left< f_1, \ldots, f_n \right>$, we present a practical and efficient method to compute the algebraic structure of the quotient algebra $ A = \mathbb{Q}[x]/\left< f\right>$. The whole method consists in matrix calculations. A set of experiments illustrate the method's effectiveness. 

\end{abstract}


\end{document}
